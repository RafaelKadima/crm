import { AdsKnowledgeBase } from '@/pages/ads'

/** Wrapper — relocaliza pra /settings/ai/knowledge.
 *  Apesar do nome do arquivo original (AdsKnowledgeBase), o
 *  conteúdo é a base de conhecimento RAG geral dos agentes,
 *  não específica de Ads. */
export { AdsKnowledgeBase as KnowledgePage }
