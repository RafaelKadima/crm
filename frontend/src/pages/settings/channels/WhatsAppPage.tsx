import { ConnectChannelsPage } from '@/pages/channels/ConnectChannelsPage'

/** Wrapper — relocaliza pra /settings/channels/whatsapp. UI de toggle
 *  OAuth proxy (Sprint 5 backend) virá após o refactor de
 *  MetaOAuthService aterrar. */
export { ConnectChannelsPage as WhatsAppPage }
