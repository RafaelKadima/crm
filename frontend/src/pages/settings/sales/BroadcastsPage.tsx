import { BroadcastsPage as ExistingBroadcastsPage } from '@/pages/broadcasts/BroadcastsPage'

/** Wrapper — relocaliza pra /settings/sales/broadcasts. UI de send-window
 *  picker (Sprint 5 backend) virá em sprint futura — backend já aceita
 *  os campos via API existente. */
export { ExistingBroadcastsPage as BroadcastsPage }
